<Window x:Class="PathEditor.MainWindow1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PathEditor"
        
        xmlns:uc1="clr-namespace:PathEditor.Views.UserControls;assembly=PathEditor"
        mc:Ignorable="d"
        Title="MainWindow1" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <uc1:AutoCompleteTextBox Grid.Column="0"  x:Name="txt"
                    Style="{StaticResource PathTextBoxStyle}"
                    Text="{Binding Path=SelectedPathPart.Path, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                    AutoCompleteCommand="{Binding AutoComplete}" />
        <!--IsEnabled="{Binding SelectedPathPart, Converter={StaticResource TextBoxEnabledConverter}}"-->
    </Grid>
</Window>
